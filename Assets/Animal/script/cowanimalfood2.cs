using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class cowanimalfood2 : MonoBehaviour
{
    public int hunggerneed;
    // Start is called before the first frame update
    void Start()
    {
        if (hunggerneed == 200)
        {
            kenyang();
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void kenyang()
    {
        //gameObject.destroy
    }
}
